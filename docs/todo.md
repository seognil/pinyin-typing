# 待办

- 数据相关说明：见 `docs/data.md`
- 训练讨论笔记：见 `docs/training.md`

## 已完成的数据工作

- [x] 把默认数据管线收敛到当前产品真正使用的 sources
- [x] 把完整 `8105` 单字 tier 以 shard 形式导出到 `data/shards/`
- [x] 采用 `edu + modern + phrase` 作为词语 tiers 模型
- [x] 通过 `modern` 相对 `edu` 去重、`phrase` 相对前两层去重，保持三个词语 tiers 互斥
- [x] 重建统一的全局词语 `fr`，供运行时训练使用
- [x] 在 `data/manifest.json` 中记录课程分桶范围
- [x] 把脏数据复核导出保留在 `.source/output-exclude/`
- [x] 把可复用的混淆规则吸收到代码中

## 当前方向

- 项目保持小而快，方便持续迭代。
- 词语训练是主模式。
- 单字训练负责覆盖和补强。
- 课程主干使用 `edu-1 -> edu-2 -> edu-3 -> edu-4 -> modern -> phrase`。
- 运行时行格式继续保持紧凑，把训练策略信息尽量放到 manifest 和运行时状态里。

## 下一阶段产品工作

- 基于 `data/manifest.json` 的课程分桶和 shard 元数据实现运行时加载器。
- 增加词级和字级掌握度跟踪。
- 让词语作答结果反哺单字掌握度。
- 实现基于训练子集的选词逻辑，并按 `半熟`、`全生`、`已打` 等业务分桶组织采样。
- 在运行时开发阶段继续细化滑动窗口和加权随机策略。
